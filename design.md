Create a website using HTML CSS and JS that allows users to upload a selection of files in SciolyFF format and displays a timeline of the tournaments side-by-side. There should be options for what information the user wants displayed.

Make a barebones website using HTML CSS and JS that creates a table of Science Olympiad tournament results based on SciolyFF files. At the top of the page, the user should be able to upload multiple tournmanent files in the SciolyFF .yaml file format. Underneath the input, each tournament should show up as a table. Each row in a tournament should have team name, rank, and total points. Each tournament table should have 20 rows for the top 20 teams, sorted by rank. The formatting for the tournament row should exactly follow the formatting on the Duosmium website, with black text on white and the different colors for the top ranked teams, as well as the star that indicates whether the team moved on to the next level of competition. Create a text file called instructions.md that gives instructions on how I should test this website locally (making sure that I can actually upload files)